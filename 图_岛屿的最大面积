/*
class Solution {
public:
    int maxArea = 0;
    int direct[4][2] = {{0, 1}, {0, -1}, {1, 0}, {-1, 0}};

    int maxAreaOfIsland(vector<vector<int>>& grid) {
        int n = grid.size(), m = grid[0].size();
        vector<vector<bool>> visited(n, vector<bool>(m, false));
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                if (grid[i][j] && !visited[i][j]) {
                    visited[i][j] = true;
                    int curArea = dfs(grid, visited, i, j, n, m);
                    maxArea = max(maxArea, curArea);
                }
            }
        }
        return maxArea;
    }

    int dfs(vector<vector<int>>& grid, vector<vector<bool>>& visited, int x,
            int y, int n, int m) {
        int curArea = 1;
        for (int i = 0; i < 4; i++) {
            int nextX = x + direct[i][0];
            int nextY = y + direct[i][1];
            if (nextX < 0 || nextX >= n || nextY < 0 || nextY >= m)
                continue;
            if (grid[nextX][nextY] && !visited[nextX][nextY]) {
                visited[nextX][nextY] = true;
                curArea += dfs(grid, visited, nextX, nextY, n, m);
            }
        }
        return curArea;
    }
};
*/

class Solution {
public:
    int direct[4][2] = {{0, 1}, {0, -1}, {1, 0}, {-1, 0}};

    int maxAreaOfIsland(vector<vector<int>>& grid) {
        int n = grid.size(), m = grid[0].size();
        vector<vector<bool>> visited(n, vector<bool>(m, false));
        int maxArea = 0;

        for (int i = 0; i < n; ++i) {
            for (int j = 0; j < m; ++j) {
                if (grid[i][j] == 1 && !visited[i][j]) {
                    int curArea = 0;
                    queue<pair<int, int>> q;
                    q.push({i, j});
                    visited[i][j] = true;

                    while (!q.empty()) {
                        auto [x, y] = q.front();
                        q.pop();
                        curArea++;

                        for (int k = 0; k < 4; ++k) {
                            int nx = x + direct[k][0];
                            int ny = y + direct[k][1];
                            if (nx >= 0 && nx < n && ny >= 0 && ny < m &&
                                grid[nx][ny] == 1 && !visited[nx][ny]) {
                                visited[nx][ny] = true;
                                q.push({nx, ny});
                            }
                        }
                    }
                    maxArea = max(maxArea, curArea);
                }
            }
        }
        return maxArea;
    }
};
