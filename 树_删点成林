/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left),
 * right(right) {}
 * };
 */

/*/////////////////////////////////////////////
class Solution {
public:
   vector<TreeNode*> ans;

   vector<TreeNode*> delNodes(TreeNode* root, vector<int>& to_delete) {
       set<int> del(to_delete.begin(), to_delete.end());
       TreeNode* tree = dfs(root, del);
       if (tree)
           ans.emplace_back(tree);
       return ans;
   }

   TreeNode* dfs(TreeNode*& root, set<int>& del) {
       if (!root)
           return root;

       TreeNode* left = dfs(root->left, del);
       TreeNode* right = dfs(root->right, del);

       if (del.count(root->val)) {
           if (left)
               ans.emplace_back(left);
           if (right)
               ans.emplace_back(right);
           root = nullptr;
           return nullptr;
       }
       return root;
   }
};
*///////////////////////////////////////////////

class Solution {
    vector<TreeNode*> ans;
    unordered_set<int> s;

public:
    TreeNode* dfs(TreeNode* root) {
        if (!root)
            return nullptr;
        root->left = dfs(root->left);
        root->right = dfs(root->right);
        if (!s.count(root->val))
            return root;
        if (root->left)
            ans.emplace_back(root->left);
        if (root->right)
            ans.emplace_back(root->right);
        return nullptr;
    }

    vector<TreeNode*> delNodes(TreeNode* root, vector<int>& to_delete) {
        for (auto x : to_delete)
            s.insert(x);
        if (dfs(root))
            ans.emplace_back(root);
        return ans;
    }
};
